#프로젝트 이름 설정 예시
#spring.application.name=basic

#서버 포트 기본 값은 8080입니다.
#server.port=8080

#spring 은 실행시 무조건 application.yml을 찾음
#active: local - application-local.yml 을 찾도록 설정
#설정 후 application-local.yml, application-prod.yml 에 config 설정 필요
spring:
  profiles:
    active: local

  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/board
    username: root
    password: test1234

